group = de.cronn

org.gradle.configuration-cache = true
org.gradle.caching = true
org.gradle.parallel = true

# Gradle's distribution always shadows the kotlin.* packages, so a bundled stdlib would be dead
# weight in the published POM. The stdlib is declared as compileOnly in build.gradle.kts instead.
kotlin.stdlib.default.dependency = false
